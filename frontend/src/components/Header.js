import React from 'react'

import { Container, Row, Col } from 'react-bootstrap'
const header = () => {
    return (
        <header>
            Header
        </header>
    )
}

export default header
